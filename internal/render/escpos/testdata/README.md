# Golden byte fixtures

This directory will hold golden byte-sequence fixtures for `escpos`
package tests (see `docs/ARCHITECTURE.md` §9) — the exact expected
ESC/POS output for a known `Canvas`. Regenerate with the test suite's
`-update` flag once it exists.
