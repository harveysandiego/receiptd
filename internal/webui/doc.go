// Package webui implements Receiptd's browser-facing HTML handlers
// (html/template plus embedded static assets from web/), backed by the
// same app.Service used by api. Arrives in Milestone 4 — see
// docs/ARCHITECTURE.md §1 and §10.
package webui
