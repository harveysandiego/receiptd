{{define "content"}}
<h2>Assets</h2>

<form method="post" action="/assets" enctype="multipart/form-data" class="upload-form">
<input type="hidden" name="csrf_token" value="{{.CSRFToken}}">
<label>Name <input type="text" name="name" required></label>
<div class="dropzone">
<label>File <input type="file" name="file" required></label>
<p class="dropzone-hint">Drag and drop a file here, or use the picker above.</p>
</div>
<button type="submit">Upload</button>
</form>

{{if .Assets}}
<div class="table-responsive">
<table class="assets">
<thead>
<tr>
<th scope="col">Preview</th>
<th scope="col">Name</th>
<th scope="col">Size</th>
<th scope="col">Modified</th>
<th scope="col">Actions</th>
</tr>
</thead>
<tbody>
{{range .Assets}}
<tr>
<td>
{{if .IsImage}}
<a href="/assets/{{.Name}}/content"><img src="/assets/{{.Name}}/content" alt="{{.Name}}" class="asset-thumb" loading="lazy"></a>
{{else}}
<a href="/assets/{{.Name}}/content">Download</a>
{{end}}
</td>
<td>{{.Name}}</td>
<td>{{.Size}}</td>
<td>{{.Modified}}</td>
<td>
<form method="post" action="/assets/{{.Name}}/delete" class="delete-form">
<input type="hidden" name="csrf_token" value="{{$.CSRFToken}}">
<button type="submit" aria-label="Delete {{.Name}}" data-confirm="Delete {{.Name}}? This cannot be undone.">Delete</button>
</form>
</td>
</tr>
{{end}}
</tbody>
</table>
</div>
{{else}}
<p>No assets stored.</p>
{{end}}
{{end}}
