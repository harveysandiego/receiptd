# web

Embedded static assets and HTML templates for `internal/webui`, arriving
in Milestone 4 (Web UI) — see `docs/ARCHITECTURE.md`. Empty until then;
this file exists so the directory is tracked by git and its purpose is
clear to anyone browsing the tree before that milestone lands.
